/* This JavaScript code is setting up a React application. Here's a breakdown of what each part is
doing: */
import React from "react";
import ReactDOM from "react-dom/client";
import "./index.css";
import App from "./App";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(<App />);
